namespace VisualProfilerAccess.Metadata
{
    enum MetadataTypes
    {
        //Backend -> Frontend
        Nothing = 10,
        AssemblyMetadata = 11,
        ModuleMedatada = 12,
        ClassMedatada = 13,
        MethodMedatada = 14,
        ProfilingData = 15
    };
}